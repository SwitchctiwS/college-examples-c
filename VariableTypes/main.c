/* 
Name: Jared Thibault
Date: 15 Jan 2015

Program Description: examining variables and experimentation*/

#include "main.h"

int main(void)
{
	float y, x = 100;
	y = SLOPE*x + INTERCEPT;
	//printf("The value of y is %f", y);
	printf("The size of a char is:%d", sizeof(unsigned int));

	return 0;
}